<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= noticeTitle %>
    </title>
    <link rel="stylesheet" type="text/css" href="<% if (typeof cssLoad !== 'undefined') { %>
        <%= cssLoad %>
    <% } else { %>
        ../css/style.css
    <% } %>">
</head>

<body>
    <div id="notice_window" class="notice-window">
        <table class="notice-container">
            <thead>
                <tr>
                    <th>
                        <%= noticeTitle %>
                    </th>
                </tr>
            </thead>
            <tbody>
                <tr>
                    <td>
                        <%= noticeContent %>
                    </td>
                </tr>
            </tbody>
            <tfoot>
                <tr>
                    <td><button onclick="location.href='<%= noticeBtn %>'">OK</button></td>
                </tr>
            </tfoot>
        </table>
    </div>
</body>

</html>